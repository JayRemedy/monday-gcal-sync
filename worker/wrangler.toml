name = "monday-jd-calendar-webhook"
main = "src/index.js"
compatibility_date = "2026-06-24"

[vars]
GITHUB_OWNER = "JayRemedy"
GITHUB_REPO = "monday-jd-calendar-sync"
MONDAY_BOARD_ID = "18419165433"
# Set this to a generated path before production, e.g. /monday-jd-calendar-<random>
WEBHOOK_PATH = "/monday-jd-calendar"

# Secrets to set with wrangler:
# wrangler secret put GITHUB_DISPATCH_TOKEN
# Optional hardening if you proxy/customize inbound calls:
# wrangler secret put INBOUND_SECRET
